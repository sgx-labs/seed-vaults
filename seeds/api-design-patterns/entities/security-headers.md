---
title: "Common Security Headers"
tags: [security, headers, csp, hsts, x-frame-options, reference, entity]
content_type: entity
domain: api-design
---

# Common Security Headers

## Essential Headers for APIs

| Header | Value | Purpose |
|--------|-------|---------|
| `Strict-Transport-Security` | `max-age=31536000; includeSubDomains` | Force HTTPS for 1 year |
| `X-Content-Type-Options` | `nosniff` | Prevent MIME type sniffing |
| `X-Frame-Options` | `DENY` | Prevent clickjacking (embedding in iframes) |
| `Content-Security-Policy` | `default-src 'none'; frame-ancestors 'none'` | Restrict resource loading |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | Control referrer information |
| `X-Request-Id` | `req_abc123` | Request tracing (generated by server) |
| `Cache-Control` | `no-store` | Prevent caching of sensitive responses |

## CORS Headers (When Needed)

| Header | Purpose |
|--------|---------|
| `Access-Control-Allow-Origin` | Which origins can access |
| `Access-Control-Allow-Methods` | Allowed HTTP methods |
| `Access-Control-Allow-Headers` | Allowed request headers |
| `Access-Control-Max-Age` | Preflight cache duration |
| `Access-Control-Allow-Credentials` | Allow cookies/auth |
| `Access-Control-Expose-Headers` | Headers the browser can read |

See `hub/cors.md` for configuration details.

## Rate Limit Headers

| Header | Purpose |
|--------|---------|
| `X-RateLimit-Limit` | Max requests in window |
| `X-RateLimit-Remaining` | Requests left in window |
| `X-RateLimit-Reset` | When the window resets (Unix timestamp) |
| `Retry-After` | Seconds to wait before retrying (on 429/503) |

See `entities/rate-limit-headers.md` for conventions.

## Deprecation Headers

| Header | Purpose |
|--------|---------|
| `Deprecation` | `true` -- this endpoint is deprecated |
| `Sunset` | `Sat, 01 Jun 2025 00:00:00 GMT` -- when it will be removed |
| `Link` | `<https://api.example.com/v2/users>; rel="successor-version"` |

## Headers to Remove in Production

| Header | Why Remove |
|--------|-----------|
| `Server` | Reveals server software and version |
| `X-Powered-By` | Reveals framework (e.g., Express, Django) |
| `X-AspNet-Version` | Reveals .NET version |

Attackers use these to target known vulnerabilities.

## Middleware Example

Apply all security headers in a single middleware:

```
function securityHeaders(response):
  response.setHeader("Strict-Transport-Security", "max-age=31536000; includeSubDomains")
  response.setHeader("X-Content-Type-Options", "nosniff")
  response.setHeader("X-Frame-Options", "DENY")
  response.setHeader("Content-Security-Policy", "default-src 'none'; frame-ancestors 'none'")
  response.setHeader("Referrer-Policy", "strict-origin-when-cross-origin")
  response.removeHeader("Server")
  response.removeHeader("X-Powered-By")
```

## See Also

- `hub/cors.md` -- CORS header configuration
- `hub/caching.md` -- Cache-Control headers
- `research/api-security-checklist.md` -- Headers in security context
